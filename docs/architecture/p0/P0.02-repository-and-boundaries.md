# P0.2 — Repository and modular boundaries

## Status block

| Field | Value |
|---|---|
| Version | `0.1` |
| Status | `PROPOSED` |
| Epic | P0.2, [manual row](../../phases/p0.html#epics): the repository, `AGENTS.md`, `badf/` registries, CI skeleton, a dependency test that fails when one module imports another's internals |
| Map ticket | [#137](https://github.com/bstBizEra/biztrust_guide/issues/137) on the [P0 design map](https://github.com/bstBizEra/biztrust_guide/issues/128), under its charting decision 8 |
| Landed by | Work Package `BIZTRUST-GUIDE-WP-076` |
| ADRs this design depends on | ADR-001 modular monolith, boundary rule and extraction criteria, `DRAFT_REQUIRED`; ADR-004 shared PostgreSQL with RLS, `DRAFT_REQUIRED`; ADR-005 contract-first APIs, `DRAFT_REQUIRED`; ADR-010 durable workflows and their runtime, `DRAFT_REQUIRED`; ADR-011 tenant packs instead of forks, `DRAFT_REQUIRED` |
| Research cited | None of the map's research files covers boundary enforcement; the stack decision [#129](https://github.com/bstBizEra/biztrust_guide/issues/129) names the candidates; tool facts are cited inline to their primary pages, checked 2026-09-06 |
| Records this design builds on | [`BIZTRUST-ARCH-001`](../BIZTRUST-ARCH-001.md) sections 4, 6 and 9; [`DOMAIN_MODEL.md`](../DOMAIN_MODEL.md) sections 4 and 6; the hub's repository design ([`index.html#repository`](../../index.html#repository)); this repository's own [`AGENTS.md`](../../AGENTS.md) and `badf/` as the pattern the platform repository carries over |

This design proposes; it builds nothing. P0 implementation waits for `BT-G0` and a Work Package with explicit, expiring authority in a platform repository, which this repository cannot grant ([`AGENTS.md`](../../AGENTS.md) sections 1 and 12). No sentence below says a capability is implemented, secure or compliant; each says what would prove it and who would record it.

## Scope and non-scope

**In scope.** The platform repository as the place every later package lands, shaped so that the modular monolith's boundaries are tested rather than intended:

1. **The workspace and its layout**: one repository, one package manager workspace, the top-level directories the hub's repository design names, and a module registry that is the single list of modules the boundary rule is generated from.
2. **The module list**, taken from the contract's capability map as candidate modules, grouped as the contract groups them, with each module's owner role and schema name.
3. **The dependency rule**: what a module may import, what it may not, and the one place a module exposes its contract.
4. **One schema per module** and the rule that a module's SQL touches only its own schema.
5. **The build-time check** that enforces the rule, the **boundary tests** that prove the check, and what a violation looks like in CI.
6. **The CI skeleton**: the jobs every pull request runs, each a required check.
7. **`AGENTS.md` and `badf/`** carried from this repository: the charter, the continuity records, their schemas and the validator that fails CI when a record does not conform.
8. **The repository administrator**, a human role, because protecting `main` is not something an agent can do (the P0 manual's entry section).

**Non-scope**, each with its owner:

- The internals of any module: each epic's design (P0.3 to P0.13). This design gives a module a place and a boundary, not code.
- Row-level security policies and the tenancy schema's tables: P0.7 and P0.6. This design fixes only that a module owns one schema and touches no other.
- API and event conventions and where their contract files live in detail: P0.8 and P0.9; this design reserves `openapi/` and `events/` as the hub's tree does.
- Security scanning, SBOM, signing and the full pipeline: the hub's security track and P3B.1. The skeleton here has the jobs a boundary needs; P0.12 adds the scanning gate.
- Whether a candidate module is a bounded context, subdomain or aggregate: `BIZTRUST-DOMAIN-001` and the bounded context map ticket ([#113](https://github.com/bstBizEra/biztrust_guide/issues/113)); the contract says its list "must not be used to justify premature service extraction" and this design uses it only as a list of boundaries to test.
- The extraction criteria for a module and the deployment trade-off: ADR-001.
- The UI: the stack decision says P0 ships no UI; `apps/` exists and is empty.
- Hosting of the repository and its CI runners: the map's fog item on hosting; this design assumes the host this guide uses (section 8, question 7).

## Authorization-sequence step

P0.2 is outside the tenant authorization sequence of [`FLOWS.md`](../FLOWS.md) section 3; it protects three of its steps by making their modules unreachable except through their contracts:

| Sequence step | What P0.2 protects there |
|---|---|
| Organization-to-tenant resolver and business authorization | The tenancy and identity-access modules' internals cannot be imported by any other module, so a business module cannot resolve a tenant or a permission by its own reading of another module's tables; it asks through the contract, which is where P0.4's mapping and the authorization decision live |
| Row-level security in PostgreSQL | A module's SQL touches only its own schema, so the trusted tenant context P0.7 proves is set and read in one place per module; a cross-schema query is a lint failure before it is a policy question |
| Audit | The audit module's write interface is its contract; no module inserts audit rows directly, which is what makes the audit record's controls (P0.10) enforceable |

## Interfaces and data shapes

### The workspace

One repository, one pnpm workspace ("A workspace must have a `pnpm-workspace.yaml` file in its root" [P1]); local packages depend on each other through the workspace protocol, under which "pnpm will refuse to resolve to anything other than a local workspace package" [P1]. The top-level layout is the hub's target repository architecture, kept as the hub draws it so that the two repositories read alike:

```text
biztrust/
├── AGENTS.md            the charter, carried from this repository and revised for a platform
├── apps/                broker, admin and partner experiences; empty in P0
├── services/            API and worker entry points
├── modules/             one workspace package per module; the boundary the rule is enforced on
│   └── modules.yaml     the module registry (below)
├── packages/            shared contracts, kernel types, observability; no domain code
├── openapi/             contract-first API specifications (P0.8)
├── events/              versioned event contracts (P0.9)
├── db/                  migrations per module schema, policies, seeds, RLS tests (P0.6, P0.7)
├── badf/                lifecycle, authority, gates, agents and skills registries; the continuity records
├── docs/                architecture, adr, product, security, operations, decisions
├── work-packages/       authorised delivery units
├── evidence/            manifests bound to a revision
├── sessions/            checkpoints and handoffs
├── schemas/             the machine-readable shape of every badf/ and evidence record
├── scripts/             deterministic validators, run by CI
├── tests/               unit, contract, security and end-to-end
└── .github/workflows/   the CI skeleton
```

The owner of the layout is the repository's charter; a directory is added by a Work Package that says why, never by a package that needed somewhere to put a file.

### The module registry

`modules/modules.yaml` is the one list of modules. The boundary rule, the schema list and the migration lint are generated from it, so a module that is not in the registry cannot import, be imported, or own a table. One entry per module:

| Field | Meaning |
|---|---|
| `name` | The package name, `@biztrust/<module>`; also the PostgreSQL schema name, with hyphens as underscores |
| `group` | `platform`, `brokerage`, `financial` or `integration`, the contract's four groups |
| `owner_role` | The role that answers for the module's contract and schema |
| `contract` | The path of the module's public entry, `src/public/index.ts`; the only path another package may import |
| `schema` | The PostgreSQL schema the module owns; `none` for a module with no persisted state |
| `epic` | The P0 or later epic whose design fills the module; a module with no epic yet is a placeholder with a contract and no code |

The initial entries are the contract's candidate modules, grouped as its capability map groups them:

| Group | Modules, as the contract lists them |
|---|---|
| Platform | Tenancy, Identity & Access, Distribution, Compliance, Audit |
| Brokerage | Party, Client, Risk, Distribution Product, Submission, Placement, Quote/Indication, Recommendation, Binding, Policy, Claims, Renewal |
| Financial | Billing, Payment, Ledger, Commission, Settlement, Reconciliation |
| Integration | Insurer, Payment Provider, Bank, Partner API, Documents, Notifications |

P0.2 creates packages for the modules P0 builds, Tenancy, Identity & Access and Audit, plus the platform-configuration module P0.12 names, and registers the rest as placeholders with a contract path and no code, so that the rule set covers every group from the first commit. Whether each candidate is a bounded context or an aggregate is not decided here (question 2).

### The dependency rule

Five rules, generated from the registry into the checker's configuration:

1. **A module's internals are private.** Anything under a module's `src/internal/` may be imported only from inside that module. The package's `exports` field exposes `src/public/` and nothing else, so an import of an internal path fails at resolution as well as at the check.
2. **Modules depend on contracts.** A module may import another module only through `@biztrust/<module>`, which resolves to that module's `contract` path. What a contract exports is the module's commands, queries, published event types and identifier types; never a repository, a table or a connection.
3. **No cycles.** The module dependency graph is acyclic. A pair of modules that need each other is one module or a missing contract; the check says which pair.
4. **Shared code is not domain code.** `packages/*` may be imported by any module and may import no module. A package that needs a module's type has found domain code in the wrong place.
5. **Entry points and experiences see contracts only.** `services/*` and `apps/*` import modules' contracts and `packages/*`; nothing imports `services/*` or `apps/*`.

The check runs at build time on every pull request. Two candidates from the stack decision are named in section 7; both express these rules as forbidden or allowed dependencies between path patterns, which is why the registry, not the checker, is the source of the rules.

### One schema per module

Every module with persisted state owns one PostgreSQL schema, named in the registry, and no other. The rules a migration lint enforces, drawn from the contract's data-ownership section ("Every authoritative datum has one owner ... Cross-module direct table mutation is prohibited. The owning module changes its state through a command and publishes resulting facts through contracts.") and the domain model's aggregate rules ("An aggregate has exactly one owning module. Only the owner executes its commands and persists its invariant-bearing state. Other modules reference stable identifiers or consume versioned facts."):

- A migration under `db/migrations/<module>/` creates, alters or drops objects in that module's schema only.
- No foreign key crosses a schema boundary; another module's aggregate is referenced by its stable identifier, and the reference's integrity is the owning module's contract to keep.
- Every tenant-owned table carries the tenant-owned record baseline of the domain model's section 6, `tenant_id` first; whether `tenant_id` is physically repeated on child rows is `BIZTRUST-DATA-001`'s decision and P0.6's to carry.
- A module's runtime SQL names only its own schema; the lint over migrations is the build-time half, and P0.7's negative controls are the runtime half.

### The CI skeleton

One workflow, every job a required check on `main`, in this order; a later job does not run if an earlier one fails:

| Job | What it runs | What a failure looks like |
|---|---|---|
| Install | The package manager with a frozen lockfile | A changed lockfile that the pull request did not commit |
| Validate records | The continuity validator over `badf/` and `sessions/`, carried from this repository | A state file, action or checkpoint that does not conform to its schema, named by path and field |
| Typecheck and lint | The compiler and the linter across the workspace | The first error with file and line |
| Boundary check | The checker's rules generated from the registry | The rule's name, the importing file and the imported path, one line per violation, and a non-zero exit |
| Migration lint | The schema-per-module rules over `db/migrations/` | The migration file and the object it creates outside its schema |
| Unit and boundary tests | The test runner, including the tests of section 5 | The failing test's name and assertion |
| Build | Every package builds | The package and the error |

A violation therefore reads, in the pull request's checks, as one named rule broken by one file, and the pull request cannot merge until the check is green. There is no override label; a rule that must be relaxed is changed in the registry by a pull request that says why.

### `AGENTS.md` and `badf/`

The platform repository's charter is this repository's charter revised for code: the same precedence order, resume protocol, sources of truth, checkpoint and handoff contracts, state transitions, evidence requirements and stop conditions, with the work constraints rewritten for a repository that ships software. The five registries the hub names, `lifecycle.yaml`, `authority.yaml`, `gates.yaml`, `agents.yaml` and `skills.yaml`, sit under `badf/` beside the continuity records, each with a schema under `schemas/` and each validated by the same job that validates the records. The authority registry is where a Work Package's implementation grant, with its expiry, is recorded before any code lands, which is the rule this guide's manuals state and the platform's CI can then read.

### The repository administrator

A human role, named in the P0.2 Work Package, holding what no agent may: protection of `main` with the seven checks above required, force-push and deletion blocked, and one independent approval required for every merge; the repository's secrets for CI, held per P0.12's inventory; and the first grant of implementation authority for P0.2 itself, recorded in the authority registry with its expiry. The plan's seat table names a repository administrator seat with this responsibility; who holds it is the review-seat register's (#94).

## Negative controls

Each control is seen failing before the protection exists and passing after; the verifier is not the implementer.

| # | Threat or defect | Condition | Expected denial or failure | Seen failing at |
|---|---|---|---|---|
| 1 | A module reaches into another's internals | A file in the Tenancy module imports `@biztrust/audit/src/internal/...` | The package resolution refuses the path and the boundary check names rule 1, the file and the import; the pull request cannot merge | Before the `exports` field and the rule: the import resolves and CI is green |
| 2 | A cycle between two modules | Identity & Access imports Tenancy's contract and Tenancy imports Identity & Access's | The boundary check names rule 3 and the pair | Before the rule: both imports resolve |
| 3 | An entry point bypasses a contract | A file in `services/api` imports a module's internal repository class | The boundary check names rule 5 and the file | Before the rule: the import resolves |
| 4 | A migration writes outside its schema | A migration under `db/migrations/tenancy/` creates a table in the `audit` schema | The migration lint names the file and the object | Before the lint: the migration applies |
| 5 | A foreign key crosses a schema | A migration adds a foreign key from a tenancy table to an audit table | The migration lint names the constraint | Before the lint: the migration applies |
| 6 | Code lands without the checks | A push straight to `main`, and a pull request merged with a failing boundary check | The host refuses the push and the merge; the refusal is the administrator's evidence | Before protection: the push lands |
| 7 | A module exists outside the registry | A package is added under `modules/` with no registry entry | The boundary check's generation step fails, naming the package | Before the generation step: the package builds |
| 8 | A record drifts from its schema | A checkpoint is committed with a missing field | The validate-records job fails, naming the file and the field | Before the validator runs in CI: the commit is green |

## Evidence contract

A control run or the repository's acceptance records, per [`AGENTS.md`](../../AGENTS.md) section 9; a record missing a field is not evidence:

| Field | Content for P0.2 |
|---|---|
| Repository and commit SHA | The platform repository and the commit at which the registry, the rules and the workflow were read |
| Command or workflow identity | The workflow file and run identifier for each job, or the pull request whose check produced the failure |
| Execution time and environment | Start and end, runner image, Node.js and package manager versions, the checker's version, PostgreSQL version for the migration lint |
| Exit status | Per job and per control |
| Artifact or output reference | The check run's log line naming the rule, file and import for controls 1, 2, 3 and 7; the lint's output for 4 and 5; the host's refusal message and the protection settings export for 6; the validator's output for 8 |
| Verifier role | The named role that ran or observed each control, not the implementer; for control 6 the repository administrator, who is a human |
| Declared coverage | Which rules, modules and jobs the run covered |
| Declared non-coverage | Modules registered as placeholders with no code, and any rule not exercised by a control |

## Technology candidates

| Candidate | Candidate for | ADR and register status | Research or source | Exit path if rejected after use |
|---|---|---|---|---|
| pnpm workspaces | The one workspace and the workspace protocol between local packages | ADR-001, `DRAFT_REQUIRED`; the stack decision #129 | pnpm's workspace page [P1] | Another workspace manager reads the same `packages/` and `modules/` layout; the registry does not change |
| dependency-cruiser | The build-time boundary check | ADR-001, `DRAFT_REQUIRED`, whose minimum evidence names boundary tests | Its README: "Validate and visualise dependencies. With your rules."; rules as `forbidden` and `allowed` entries with `from` and `to` path patterns; reports violated rules and fails the build with a non-zero exit; TypeScript supported; MIT [T1] | The rules are generated from the registry, so a different checker consumes the same registry |
| eslint-plugin-boundaries | The same check, inside the linter | ADR-001, `DRAFT_REQUIRED` | Its README: "Enforce architectural boundaries in your JavaScript and TypeScript projects"; element types by path pattern and a `boundaries/dependencies` rule with `default: "disallow"` and allow policies; MIT [T2] | Same as above |
| NestJS on the Fastify adapter | The module and dependency-injection shape the rule is enforced against | ADR-001, `DRAFT_REQUIRED`; the stack decision | The stack decision's reasons | A module's contract is a plain package export; the framework's module system is a second boundary, not the first |
| Vitest | The unit and boundary tests | ADR-001, `DRAFT_REQUIRED`; the stack decision | The stack decision | Any runner runs the same tests |
| PostgreSQL schemas, one per module | The storage boundary | ADR-004, `DRAFT_REQUIRED` | The contract's section 9 and the domain model's section 4 | A per-module database would keep the lint's rule and change its object names |

Two things this design needs have no ADR row and no research and are questions, not candidates: the migration lint, which section 8 assumes to be a script under `scripts/` like this repository's validator, and the repository host with its branch protection, which the design assumes to be the host this guide uses.

## Open questions and dependencies

1. **ADR-001 decides the style, the boundary rule and the extraction criteria.** Waits on: [#103](https://github.com/bstBizEra/biztrust_guide/issues/103) on the contract map, behind the waiver (#92). `ASSUMED` meanwhile: the contract's proposed decision, a modular monolith behind explicit module contracts, with the five rules above as the boundary rule; if the ADR narrows or widens them, this design is revised under a new version.
2. **Whether a candidate module is a bounded context, a subdomain or an aggregate.** Waits on: `BIZTRUST-DOMAIN-001` and the bounded context map ([#113](https://github.com/bstBizEra/biztrust_guide/issues/113)). `ASSUMED` meanwhile: the capability map's candidate modules are the registry's entries, one package each, and a merge or split of two candidates is a registry change under the map's decision.
3. **One schema per module against the physical tenant-key decision.** Waits on: ADR-004 ([#105](https://github.com/bstBizEra/biztrust_guide/issues/105)) and `BIZTRUST-DATA-001`. `ASSUMED` meanwhile: one schema per module with `tenant_id` on every tenant-owned table, as the domain model's baseline lists it first.
4. **Which checker.** Waits on: ADR-001, whose evidence names boundary tests but not the tool. `ASSUMED` meanwhile: dependency-cruiser for the shape of the generated rules, since its `forbidden` entries map one to one onto rules 1 to 5; the registry keeps the switch cheap.
5. **The migration lint.** No research names a tool that checks a migration's objects against a schema list. `ASSUMED` meanwhile: a deterministic script under `scripts/`, parsing migration SQL for `CREATE`, `ALTER`, `DROP` and `REFERENCES` targets, in the pattern of this repository's continuity validator; a tool that does the same is a later candidate under ADR-004.
6. **Where durable workflows live.** Waits on: ADR-010 ([#109](https://github.com/bstBizEra/biztrust_guide/issues/109)). `ASSUMED` meanwhile: a workflow is a module's code that calls other modules' contracts; a workflow runtime, when chosen, is infrastructure behind `packages/`, and no module imports the runtime's internals.
7. **The repository host and its protection.** No ADR names the host. `ASSUMED` meanwhile: the host this guide runs on, with the branch protection this guide's charter describes; the repository administrator is the seat the plan names, filled through #94.
8. **The charter's revision for a platform.** The platform's `AGENTS.md` is this repository's charter with its work constraints rewritten for code. Waits on: the P0.2 Work Package's own review. `ASSUMED` meanwhile: the sections and their order are kept, so that a reader of one charter can read the other.

No item is `UNKNOWN_BLOCKING`: the design proceeds on its assumptions and each is reversible by the decision it names.

## Work Package cut

Two packages, in this order, under the manual's one-primary rule: the first is primary because nothing else in P0 has anywhere to land before it, and the manual's execution order puts P0.2 first for that reason.

| Package | Bounded outcome | Owner role | Verifier role | Acceptance criteria | Rollback |
|---|---|---|---|---|---|
| P0.2-A, first | The repository: the workspace and layout, the charter and `badf/` with schemas and the validator, the module registry with every candidate registered and four packages created, the dependency rule generated from the registry, the CI skeleton with its seven required checks, and `main` protected by the repository administrator | Engineering, with the repository administrator as the human role | The peer reviewer role, not the implementer; the repository administrator for control 6 | Controls 1, 2, 3, 6, 7 and 8 seen failing then passing with the evidence fields recorded; the registry lists every candidate module of the contract's capability map; the validator passes on the repository's own records | The repository is new and nothing depends on it: rollback is to delete the branch that introduced the change, or the repository itself before its first accepted package |
| P0.2-B | One schema per module: the migration layout under `db/migrations/<module>/`, the migration lint, and the first schema created for the tenancy module once P0.6 names its tables | Engineering | The data reviewer role, not the implementer | Controls 4 and 5 seen failing then passing; the lint runs as a required check | Drop the schema by the same migration tooling; the lint is a script that can be removed by a pull request that says why |

Each package carries its own expiring implementation authority from the business authority seat, recorded in the platform's authority registry and, until that registry exists, on the package's issue; each names its verifier before it starts; and each records its evidence per section 6 before it claims its outcome.

## Sources for the tool facts

All checked 2026-09-06.

- [P1] pnpm, Workspace: https://pnpm.io/workspaces
- [T1] dependency-cruiser, README: https://github.com/sverweij/dependency-cruiser/blob/main/README.md
- [T2] eslint-plugin-boundaries, README: https://github.com/javierbrea/eslint-plugin-boundaries/blob/master/README.md
